import * as React from 'react'

export * from './types'

declare const _default: {
    crollToPage: (index: number | null, animated = true) => void,
    scrollToNextPage: (animated = true) => void,
}

export default _default
